import {UserDao} from "../src/daos/userDao"

/*test('get user', () => {
   userDao : new UserDao();
   expect(userDao.getUser("oliverlewis"))
});*/

/*
test('sample', () => {
    expect(sum(1,2)).toBe(3);
});*/
